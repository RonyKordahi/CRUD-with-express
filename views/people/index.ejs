<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>People</title>
</head>

<body>
    <h1>List of People:</h1>
    <ul>
        <% people.forEach((person)=> { %>

            <li>
                <h2>
                    <a href="http://localhost:8000/people/<%= person.id %>">
                        <%= person.name %>
                    </a>
                    <a href="http://localhost:8000/people/edit/<%= person.id %>">
                        EDIT
                    </a>
                    <a href="http://localhost:8000/people/delete/<%= person.id %>">
                        DELETE
                    </a>
                </h2>
            </li>

            <% }) %>
    </ul>
</body>

</html>